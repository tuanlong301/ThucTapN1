<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/lotte"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lotte"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBanHang"
        tools:targetApi="31"
        tools:ignore="ExtraText"
        android:name=".common.base.MyApp">

        <!-- Login (Launcher) -->
        <activity
            android:name=".feature.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- User -->
        <activity
            android:name=".feature.user.MenuActivity"
            android:exported="false"
            android:screenOrientation="landscape"/>

        <activity
            android:name=".feature.user.CartActivity"
            android:exported="false"
            android:screenOrientation="landscape"/>

        <!-- Admin -->
        <activity
            android:name=".feature.admin.AdminMenuActivity"
            android:exported="false"
            android:screenOrientation="landscape"/>

        <!-- Owner -->
        <activity
            android:name=".feature.owner.OwnerMenuActivity"
            android:exported="false"
            android:screenOrientation="landscape"/>

        <!-- Offline -->
        <activity
            android:name=".common.network.OfflineActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:screenOrientation="landscape"/>

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
</manifest>